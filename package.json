{
  "name": "cod-licencias",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor de licencias del Formulario COD (genera y valida llaves).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.130.0"
  }
}
